name: sarif
description: SARIF 2.1.0 (Static Analysis Results Interchange Format) data structures and builder for Dart.
version: 0.1.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  json_annotation: ^4.10.0

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  test: ^1.25.0
